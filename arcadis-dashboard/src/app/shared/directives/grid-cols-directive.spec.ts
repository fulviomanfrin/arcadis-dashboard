import { GridColsDirective } from './grid-cols-directive';

describe('GridColsDirective', () => {
  it('should create an instance', () => {
    expect(new GridColsDirective()).toBeTruthy();
  });
});
